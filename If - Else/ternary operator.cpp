#include <iostream>
using namespace std;
int main() {

// Ternary operator is used to replace the if-else statement

 // int n;
 // cin>>n;
 // if (n%2==0) { cout << "even number";}
// else { cout << "odd number";}

 // (condition) ? (true statement) : (false statement); // ternary operator

 
   int n;
   cout << "enter a number: ";
   cin >> n;
    (n%2==0) ? cout << "even number" : cout << "odd number"; // ternary operator

}